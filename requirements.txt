# Telegram (MTProto — supports media up to 2 GB without a Local Bot API server)
telethon>=1.36,<2
cryptg>=0.4.0          # C-accelerated AES for fast large-file downloads

# Speech recognition (CTranslate2 Whisper: CPU-efficient, CUDA-ready,
# word-level timestamps + built-in VAD — WhisperX-class alignment quality)
faster-whisper>=1.0.3

# Translation transport (any OpenAI-compatible API)
httpx>=0.27.0

# Configuration
python-dotenv>=1.0.1
